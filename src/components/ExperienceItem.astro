---

interface Props {
  title: string
  company: string
  description: string
  link?: string
  date: string
}

const { title, company, description, link, date } = Astro.props

import { getI18N } from "../i18n";
const { currentLocale } = Astro;
const i18n = getI18N({ currentLocale });

---

<div
  class="relative mx-12 pb-12 grid before:absolute before:left-[-35px] before:block before:h-full before:border-l-2 before:border-primary-100 before:content-[''] md:grid-cols-5 md:gap-10 md:space-x-4]"
>
  <div class="relative pb-12 md:col-span-2">
    <div class="sticky top-0">
      <span class="text-primary-700 -left-[42px] absolute rounded-full text-5xl"
        >&bull;</span
      >

      <h3 class="text-xl font-bold text-primary-400">{title}</h3>
      <h4 class="font-semibold text-xl text-secondary-100">{company}</h4>
      <time class="p-0 m-0 text-sm text-secondary ">{date}</time>
    </div>
  </div>
  <div class="relative flex flex-col gap-2 pb-4 text-secondary md:col-span-3">
    {description}
    {
      link && (
      <a href={link} role="link" class="inline-flex items-center text-lg font-medium text-primary   hover:underline">
        {i18n.btn[1]}
        <svg xmlns="http://www.w3.org/2000/svg" width="1.2rem" height="1.2rem" viewBox="0 0 24 24">
            <path fill="#64ffda" d="M10 6L8.59 7.41L13.17 12l-4.58 4.59L10 18l6-6z" />
        </svg>
      </a>
      )
    }
    
  </div>
</div>
